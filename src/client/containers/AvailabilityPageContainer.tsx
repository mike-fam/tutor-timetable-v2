import React from "react";
import { AvailabilityTimetableContainer } from "./AvailabilityTimetableContainer";

type Props = {};

export const AvailabilityPageContainer: React.FC<Props> = ({}) => {
    return <AvailabilityTimetableContainer />;
};
