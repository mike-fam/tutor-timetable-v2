query Terms {
    terms {
        id
        type
        year
        startDate
        endDate
        weekNames
    }
}

query Term($termId: Int!) {
    term(termId: $termId) {
        id
        type
        year
        startDate
        endDate
        weekNames
    }
}
