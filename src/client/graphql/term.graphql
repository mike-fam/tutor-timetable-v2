query Terms {
    terms {
        id
        type
        year
        startDate
        endDate
        weekNames
        isActive
    }
}

query Term($termId: String!) {
    term(termId: $termId) {
        id
        type
        year
        startDate
        endDate
        weekNames
        isActive
    }
}
