mutation AcceptOffer(
    $offerId: Int!
    $requestId: Int!
    $offerSessionSwapId: Int
) {
    acceptOffer(
        offerId: $offerId
        requestId: $requestId
        offerSessionSwapId: $offerSessionSwapId
    )
}
