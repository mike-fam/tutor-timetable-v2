export enum RequestType {
    PERMANENT = "Permanent",
    TEMPORARY = "Temporary",
}

export enum RequestStatus {
    OPEN = "Open",
    CLOSED = "Closed",
}

export enum FormType {
    CREATE = "Create",
    APPLY = "Apply",
}
